import{NextRequest,NextResponse}from'next/server';import{getPrincipalFromRequest}from'@/lib/auth';import{query}from'@/lib/db';export async function GET(req:NextRequest){const p=await getPrincipalFromRequest(req);if(!p)return NextResponse.json({error:'Unauthorized'},{status:401});const orgIds=p.memberships.map(m=>m.organizationId);const r=await query(`SELECT r.*,o.name source_name FROM revenue_funnel_snapshots r LEFT JOIN organizations o ON o.id=r.source_organization_id WHERE r.organization_id=ANY($1::uuid[]) ORDER BY period_end DESC,submitted_count DESC LIMIT 250`,[orgIds]);return NextResponse.json({funnels:r.rows});}
